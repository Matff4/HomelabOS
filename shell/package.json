{
  "name": "homelabos-shell",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:sdk": "npm --prefix ../sdk install && npm --prefix ../sdk run build",
    "build": "vite build && npm run build:sdk",
    "preview": "vite preview"
  },
  "dependencies": {
    "gridstack": "^11.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
